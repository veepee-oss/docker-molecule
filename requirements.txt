# https://github.com/ansible-community/ansible-build-data/blob/main/8/CHANGELOG-v8.rst#id583
ansible-core==2.15.6
ansible-compat==4.1.10
ansible==8.6.1
docker==6.1.3
jmespath==1.0.1
# not the real version, check .gitlab-ci.yml#MOLECULE_VERSION
# we keep it here so it's still usable locally when testing
molecule==6.0.2
molecule-docker==2.1.0
netaddr==0.10.0
testinfra==6.0.0
pytest==7.4.3
